# Secrets — never commit. Use .env.example as the template.
.env

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/

# Nova local audit trail + any synced TaskFlow data must never leave the machine
nova_audit.log
.taskflow/
# Ignore real TaskFlow exports, but KEEP the committed sanitized demo samples.
tasks.json
!docs/sample_taskflow/tasks.json
!nova/seed/tasks.json

# Internal working notes — never publish (contain audit/optics notes + local paths)
docs/RE_AUDIT_BRIEFING.txt
docs/*_BRIEFING.txt

# OS / IDE junk
.DS_Store
Thumbs.db
.vscode/
.idea/
